import React from 'react';

import { MobileMenu } from '@equisoft/design-elements-react';

export default {
    title: 'Mobile Menu',
    component: MobileMenu,
};

export const normal = () => (
    <MobileMenu/>
);
