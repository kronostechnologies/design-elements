import { Spinner } from '@equisoft/design-elements-react';
import { StoryFn as Story } from '@storybook/react';

export default {
    title: 'Notification/Spinner',
    component: Spinner,
};

export const Enso: Story = () => (
    <Spinner />
);
