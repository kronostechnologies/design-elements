import { Card } from '@equisoft/design-elements-react';
import * as React from 'react';

export default {
    title: 'Card',
    component: Card,
};

export const normal = () => (
    <Card>Hello, World!</Card>
  );
