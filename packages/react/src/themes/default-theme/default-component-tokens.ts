import { ComponentTokenMap } from '../tokens';
import { defaultButtonTokens } from './default-component-tokens/default-button-tokens';

export const defaultComponentTokens: ComponentTokenMap = {
    ...defaultButtonTokens,
};
